# Bubble Vault — ไฟล์กลางของโปรเจกต์

อ่านไฟล์นี้ก่อนแตะโค้ด: มันบอกว่าเว็บนี้*ตั้งใจจะเป็นอะไร* ไม่ใช่แค่ทำอะไรได้บ้าง
(ความสามารถรายข้ออยู่ใน `README.md`; กติกาการเขียน Next อยู่ใน `AGENTS.md`)

## เป้าหมาย: "หลังบ้านของร้าน Bubble Shop" ในเว็บเดียว

ร้านขาย Game Pass / Robux / ฟาร์ม ใน Roblox ผ่านแชต ทีมงานทำงานคนละเครื่อง
งานทุกวันคือ **รับออเดอร์ → ส่งของในเกม → แคปหลักฐาน → ตอบลูกค้า** และของที่
ต้องใช้ระหว่างทางกระจัดกระจาย: รูปสินค้าอยู่ที่หนึ่ง หลักฐานอยู่ในเครื่องพนักงาน
เครื่องมือย่อรูป/แก้ข้อความอยู่บนเว็บคนอื่น

เว็บนี้จึงกำลังเติบโตจาก "ที่ฝากรูป" เป็น **หลังบ้านสามเสา**:

| เสา | คืออะไร | สถานะ |
|---|---|---|
| **คลังรูป** | ฝากรูปสินค้า/โปรโมต ได้ลิงก์ตรงเอาไปแปะที่ไหนก็ได้ เสิร์ฟผ่าน R2 + CDN | ใช้งานอยู่ |
| **หลักฐานส่งของ** | รูปจากโปรแกรม Bubble Capture บนเครื่องพนักงาน ไหลเข้ามาเองพร้อมชื่อผู้ส่ง ชื่อลูกค้า ประเภทออเดอร์ เวลา และตราประทับ ค้นย้อนหลัง 90 วัน | ใช้งานอยู่ |
| **เครื่องมือ** | ของจิปาถะที่ทีมใช้ทุกวัน ทำงานในเบราว์เซอร์ทั้งหมด: ย่อรูป · ใส่โลโก้ · ครอปสัดส่วน · ค้นหา&แทนที่ · เทมเพลตข้อความ · คำนวณราคา · QR ลิงก์ | ครบชุดแรกแล้ว |

ปลายทางคือ: พนักงานเปิดแท็บเดียวแล้วทำงานทั้งวันได้ เจ้าของเปิดจากมือถือแล้วรู้ว่า
ร้านเป็นยังไง (ส่งไปกี่ออเดอร์ ใครส่ง มีอะไรค้าง) โดยไม่ต้องถามใคร

## หลักที่ยึด (ถ้าขัดกับฟีเจอร์ใหม่ ฟีเจอร์แพ้)

1. **ไม่เพิ่มภาระงาน** — ทุกอย่างที่ทำซ้ำต้องจำค่าล่าสุด กดครั้งเดียวจบ ถ้าฟีเจอร์ต้องกดเพิ่มทุกออเดอร์ ให้คิดใหม่
2. **เครื่องมือทำงานในเบราว์เซอร์** ไม่แตะเซิร์ฟเวอร์/R2 — Railway มี RAM 384 MB และรูปอยู่บนเครื่องผู้ใช้อยู่แล้ว
3. **ไม่รกคลัง** — ผลลัพธ์ของเครื่องมือคือ *คัดลอก* หรือ *ดาวน์โหลด* ไม่ใช่เก็บเข้าคลังโดยอัตโนมัติ
4. **ทุกคนในทีมใช้เครื่องมือได้** แต่แก้/ลบ/จัดการทีมได้เฉพาะเจ้าของ (role `owner` vs `staff`)
5. **หลักฐานแก้ไม่ได้** เปลี่ยนได้แค่ชื่อลูกค้ากับประเภท (แก้พิมพ์ผิด) — นั่นคือคุณค่าของมัน
6. **สีเดียว** ม่วงของร้านเป็นสีธีม สีอื่นมีได้เมื่อ*มีความหมาย* (เขียว = สำเร็จ/ประหยัด, แดง = ผิดพลาด) ไม่ใช่เพื่อความสวย — เจ้าของเคยขอ "เพิ่มสีนิดเดียว" และปัดสีรุ้งทิ้ง
7. **โปรแกรมกับเว็บต้องรู้สึกเป็นของชิ้นเดียว** ใช้ token สี/ระยะ/ฟอนต์เดียวกัน (โปรแกรมก๊อปจาก `app/globals.css`)

## โครงสร้าง (แผนที่สั้น ๆ)

```
app/page.tsx, app/tools/*      route ทั้งหมดเรนเดอร์เปลือกเดียวกัน (app/shell.tsx → VaultApp)
app/components/VaultApp.tsx    state หลัก: หมวดที่เปิด (active) · คลัง · หลักฐาน · แจ้งเตือน · URL ↔ active
app/components/Rail.tsx        เมนูซ้าย: คลัง · หลักฐาน (รายคน) · เครื่องมือ (จาก lib/tools.tsx)
app/components/CapturesView    หลักฐาน: กรองประเภท/คน/ค้นหา 90 วัน · จัดกลุ่มตามวัน
app/components/ShrinkTool      เครื่องมือย่อรูป (lib/shrink.ts = engine + zip)
lib/tools.tsx                  รายชื่อเครื่องมือ *ที่เดียว* — เพิ่มเครื่องมือ = เพิ่ม 1 entry + component ใน tools-registry
lib/captures.ts                แคตตาล็อกหลักฐานรายเดือน (_captures/<hex>/YYYY-MM.json บน R2) + sidecar ต่อรูป
app/api/{pair,me,capture,...}  API สำหรับโปรแกรม Bubble Capture (bearer token ต่อเครื่อง)
lib/session.ts, lib/auth.ts    cookie เซ็นชื่อ มี role · requireAuth / requireOwner
proxy.ts                       ด่านหน้า: อะไรสาธารณะ อะไรต้อง cookie อะไรใช้ bearer
```

โปรแกรมเดสก์ท็อปอยู่คนละ repo: `officialfontz/bubble-capture` (Tauri 2, Mac+Windows) อัปเดตตัวเองจาก R2

## การตัดสินใจที่ล็อกแล้ว (อย่าเปิดใหม่โดยไม่ถามเจ้าของ)

- หลักฐานเก็บ 90 วันแล้วลบเอง · จับคู่เครื่องด้วยรหัส 6 ตัว · 1 เครื่อง = 1 พนักงาน
- metadata ของหลักฐานมีแค่ ชื่อลูกค้า + ผู้ส่ง + เวลา + ประเภท (Game Pass/Robux/ฟาร์ม) ไม่ผูกเลขออเดอร์
- ทุกคนเห็นหลักฐานของทุกคน ไม่มีอะไรเป็นความลับในทีม
- แจ้งเตือนบนเว็บเมื่อมีหลักฐานใหม่ / เครื่องพนักงานมีรูปค้าง (เสียง + ป้าย + ชื่อแท็บ) ปิดได้ต่อเบราว์เซอร์
- ค่าเริ่มต้นเครื่องมือย่อรูป: ไม่เกิน 500 KB, WebP, คัดลอกเป็นปุ่มหลัก

## ถัดไป (เรียงตามคุ้ม)

ชุดแรกครบ 9 ก.ย. 2569: ใส่โลโก้ · ครอปสัดส่วน · เทมเพลตข้อความ · คำนวณราคา (Robux ÷ เรต ปัดขึ้น) · QR ลิงก์
ต่อไป: **อ่านราคาจากรูปหน้าร้านในเกม** (ต้องใช้ AI อ่านภาพ → รายการ + Robux → คำนวณราคาให้เอง) · ลบพื้นหลัง
- คลังรูปยังส่งทั้งก้อนตอนเปิดหน้า (~500 ไบต์/รูป) ทำแบ่งหน้าเมื่อใกล้ 2,000 รูป
- ใบรับรองเซ็นโค้ดของโปรแกรม (ตัด SmartScreen) เมื่อทีมโตกว่านี้

## วิธีทำงานกับโค้ดนี้

- `npm run validate` (tsc + eslint) ต้องผ่านศูนย์ warning ก่อน commit; ก่อน push ที่แตะ route ให้รัน `next build` ในสำเนา
- เขียนคอมเมนต์อธิบาย*ทำไม* ไม่ใช่ทำอะไร และเล่าบั๊กที่เคยเกิดตรงนั้น — โค้ดเบสนี้ทำแบบนั้นทั้งหมด
- ข้อความในหน้าจอเป็นไทยที่พนักงานร้านอ่านรู้เรื่อง ไม่ใช่ศัพท์เทคนิค
- push `main` = deploy ขึ้น Railway ทันที (เว็บร้านที่ใช้จริง) ถ้าไม่มั่นใจให้เจ้าของกดเอง
